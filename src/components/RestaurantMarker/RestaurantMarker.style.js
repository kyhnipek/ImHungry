import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {},
  image: {
    backgroundColor: 'red',
    width: 50,
    height: 50,
    borderRadius: 50,
    borderWidth: 1,
    borderColor: '#e0e0e0',
  },
});

export default styles;
